# ── Google GCP-first stack ─────────────────────────────────────────────
google-genai          # unified Gemini SDK (Google AI Studio + Vertex AI)
google-adk            # Agent Development Kit — multi-agent orchestration
litellm               # lets ADK call Gemma / external open models for the leaderboard

# ── RAG / retrieval ────────────────────────────────────────────────────
chromadb
# sentence-transformers   # optional offline-embeddings fallback
# faiss-cpu               # optional offline vector index

# ── Evaluation ─────────────────────────────────────────────────────────
textstat              # Flesch-Kincaid, SMOG readability
scikit-learn          # Cohen's kappa, agreement metrics

# ── Data / analysis ────────────────────────────────────────────────────
pandas
numpy
datasets
openpyxl              # read the MedAESQA methods .xlsx
matplotlib
seaborn

# ── Demo (December: HF Spaces or Cloud Run) ────────────────────────────
gradio
# streamlit            # alternative demo framework

# ── Optional alternatives ──────────────────────────────────────────────
# langgraph            # alternative orchestration to ADK
# groq                 # one external open model (Llama-3) for the leaderboard
